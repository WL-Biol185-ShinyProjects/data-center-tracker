# Legacy Artifacts

This folder holds historical files that are not used by the current Shiny app.

- `2008_singlefile_shrink.rds`: older compressed intermediate artifact kept only for reference

The active app lives in `App Workflow/`, and the active data refresh scripts live in `data-raw/`.
